description: Root level tasks

default_task:
  command: "echo 'Hello from bodo!'"
  description: "Root script greeting. You can run `bodo` to run this task."

tasks:
  echo:
    command: "echo 'Hello from `bodo echo`!'"
    description: "A script in the root directory. This can be run with `bodo echo`."
  echo2:
    command: "echo 'Hello from `bodo echo2`!'"
    description: "Another example script in the root directory. This can be run with `bodo echo2`."
